interface:
  display_name: "Hs Feedback"
  short_description: "用运行证据复盘 Bad Case，确认回写并完成回归验证。"
  default_prompt: "使用 $hs-feedback 读取本次运行记录，定位 Bad Case 根因，给出回写提案并设计回归验证。"
policy:
  allow_implicit_invocation: true
