interface:
  display_name: "Hs Table Builder"
  short_description: "把审计通过的结果组织为可校验的三层表格。"
  default_prompt: "使用 $hs-table-builder 把这份已审计的分析结果整理成数据源层、中间测算层和最终呈现层。"
policy:
  allow_implicit_invocation: true
